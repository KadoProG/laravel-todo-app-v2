# リポジトリ運用（AI アシスタント・共同開発者向け）

この文書は、Cursor を含む **あらゆる AI コーディングアシスタント** および人間の開発者向けに、Git ブランチの扱いを共有するためのものです。ツールに依存しない一般的なルールとして読んでください。

## Git ブランチ運用

### 現在のブランチが `main` でない場合（アシスタント向け）

- 作業の開始時、新しいブランチの作成、または `main` への切り替え、`checkout` / `rebase` / `merge` / `reset` など **ベースブランチや履歴を変える操作を行う前**に、**チェックアウト中のブランチが `main` でない場合は、必ずユーザー（開発者）に確認する。**
- 確認では次を明示する: 現在のブランチ名、`main` に切り替えてから進めてよいか、別ブランチのままにすべきか、未コミット変更の有無とその影響。
- ユーザーが了承したあとで `main` を取り込む・`main` に切り替えてから、下記の「`main` から切る」手順に従う。**ユーザーへの確認なしに `main` へ切り替えたり、履歴を書き換えたりしない。**

### 運用ルール

- **新しい作業用ブランチは必ず `main` の先端から切る。** 別のフィーチャーブランチの上にコミットを積み重ねない。
- 手順の例:

```bash
git fetch origin
git checkout main
git pull origin main
git checkout -b feat/short-description
```

- すでに別ブランチで作業中で `main` が進んでいる場合は、プルリクエストを出す前に `main` を取り込む（`git rebase origin/main` または `git merge origin/main`）。チームの慣習に合わせる。
